[project]
name = "data-pipeline-lab-sblt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=1.3.0",
    "pydantic>=2.0.0",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
addopts = "-p no:launch_testing -p no:launch_ros -p no:ament_lint -p no:ament_xmllint -p no:ament_flake8 -p no:ament_pep257 -p no:ament_copyright"
